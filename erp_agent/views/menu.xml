<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Top-level menu -->
    <menuitem id="menu_erp_agent_root" name="ERP Agent" sequence="100"/>

    <!-- Chat view — OWL component mounted here -->
    <menuitem id="menu_erp_agent_chat" name="Chat" parent="menu_erp_agent_root" sequence="10"
              action="action_erp_agent_chat"/>

    <!-- Placeholder action — replace with ir.actions.client pointing to OWL component -->
    <record id="action_erp_agent_chat" model="ir.actions.client">
        <field name="name">ERP Agent Chat</field>
        <field name="tag">erp_agent_chat</field>
    </record>
</odoo>
